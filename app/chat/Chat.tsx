type ChatProps = {
  userId: string;
};

export default function Chat({ userId }: ChatProps) {
  return (
    <div>
      {/* Sostituisci con la tua logica */}
      Chat per User ID: {userId}
    </div>
  );
}
